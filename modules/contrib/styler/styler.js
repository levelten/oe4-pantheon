(function($){
    /**
     * Mobile Navigation
     */
    Drupal.behaviors.stylerView = {
        attach: function(context, settings) {
            window.prettyPrint && prettyPrint();
        }
    }

})(jQuery);