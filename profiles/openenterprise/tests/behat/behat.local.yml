default:
  extensions:
    Behat\MinkExtension:
      base_url: http://oepro3p-test.localhost
    # uncomment the following to use Drupal API driver
    #Drupal\DrupalExtension:
      #api_driver: 'drupal'
      #drupal:
      #  drupal_root: '/path/to/drupal'
